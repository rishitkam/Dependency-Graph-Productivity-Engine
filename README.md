# Dependency-Graph-Productivity-Engine
Users create tasks as nodes and dependencies as edges.  The system automatically determines:  which tasks are Locked  which are Ready  which are In Progress  which are Blocked  The UI visualizes this as an interactive graph.
